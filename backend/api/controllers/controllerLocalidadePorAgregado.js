const modelLocalidadePorAgregado = request('../models/modelLocalidadePorAgregado');

const controllerLocalidadePorAgregado = {
    getAllTasks: (req, res) => {
        res.json(controllerLocalidadePorAgregado);
    },
};

module.exports = controllerLocalidadePorAgregado;
