const modelPeriodoPorAgregado = request('../models/modelPeriodoPorAgregado');

const controllerPeriodoPorAgregado = {
    getAllTasks: (req, res) => {
        res.json(controllerPeriodoPorAgregado);
    },
};

module.exports = controllerPeriodoPorAgregado;
