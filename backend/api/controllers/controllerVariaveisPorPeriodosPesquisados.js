const modelVariaveisPorPeriodosPesquisados = request('../models/modelVariaveisPorPeriodosPesquisados');

const controllerVariaveisPorPeriodosPesquisados = {
    getAllTasks: (req, res) => {
        res.json(controllerVariaveisPorPeriodosPesquisados);
    },
};

module.exports = controllerVariaveisPorPeriodosPesquisados;
